<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User_info">
	<select id="coffeeSensorSelectUser_info" parameterType="CoffeeUser_info" resultType="CoffeeUser_info">
		SELECT * FROM user_info WHERE ustate = 1 AND ucode != 0
   	</select>
   	<select id="coffeeRestoreSelectUser_info" parameterType="CoffeeUser_info" resultType="CoffeeUser_info">
		SELECT * FROM user_info WHERE ustate = 2 AND ucode != 0 
   	</select>
   	<select id="coffeeAccusationSelectUser_info" parameterType="CoffeeUser_info" resultType="CoffeeUser_info">
		SELECT * FROM user_info WHERE ureportcount >= 1 AND ucode != 0
   	</select>
	<select id="coffeeSensorFollowing" parameterType="int" resultType="int">
		SELECT COUNT(*) FROM follow where ucode = #{ucode}
   	</select>
   	<select id="coffeeSensorFollower" parameterType="int" resultType="int">
		SELECT COUNT(*) FROM follow where fopcode = #{ucode}
   	</select>
   	<update id="coffeeUpdateUstate" parameterType="CoffeeUser_info">
   		UPDATE user_info SET ustate = #{ustate} WHERE ucode = #{ucode}
   	</update>
   
   
   
   
   <!-- <select id="listAll3"  parameterType="Emp" resultType="Emp">
		select * from (select rownum rn, a.* 
		               from (select * from emp order by empno) a
		               ) 
		where rn between #{start} and	 #{end}   
   </select>
	<select id="listAll" parameterType="Emp" resultType="Emp">
		<![CDATA[
		select * from (select rownum rn, a.* 
		               from (select * from emp order by empno) a
		               ) 
		where rn >= #{start} 
		and	  rn <= #{end}   
		]]>
	
	</select>
   <select id="tkEmpSelOne" parameterType="int" resultType="Emp" >
       select * From emp Where empno = #{empno}   
   </select>  

   <update id="TKempUpdate" parameterType="Emp">
	 update emp set ename=#{ename}, job=#{job}, mgr=#{mgr}, hiredate=to_date(#{hiredate}, 'yy/mm/dd hh24:mi:ss'),
	    sal=#{sal}, 
	    <if test="comm!=null"> 	comm=#{comm},    </if>
	    deptno=#{deptno} where empno=#{empno}
   </update>

    <select id="selectManager" resultType="Emp">
        SELECT   * 
        FROM    emp 
        WHERE   empno in (select mgr from emp)
    </select>

   <insert id="insert" parameterType="Emp">
 	 insert into emp values (#{empno},#{ename},#{job},#{mgr},
	 to_date(#{hiredate}, 'yy/mm/dd hh24:mi:ss'),#{sal},#{comm},#{deptno} ) 
   </insert>

   <delete id="delete" parameterType="int">
	    delete from emp where empno=#{empno}
   </delete>  -->


</mapper>