<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user_info">
<select id="login" resultType="user_info" parameterType="user_info">
		SELECT *
		  FROM user_info
		 WHERE uEmail = #{uEmail}
		 AND uPassword = #{uPassword }  
	</select>
	<insert id="join">
	    INSERT INTO user_info(   uCode, uEmail, uPassword, uAtid, uNickName, uBirth,
	                             uGender, uNation, uIdentify, uChat, uAlarm, uPrivate,
	                             uRegDate, uIntro , uLoc, uImage, uBg, uProfileLink,
	                             uState, uReportCount, uOnline, uLogInCount
	     
	   					       	)
	                  VALUES(   )
   
    </insert>
	</mapper>