<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User_info">
<select id="login" resultType="user_info" parameterType="user_info">
		SELECT uEmail , uPassword
		  FROM user_info
		 WHERE uEmail = #{uEmail}
		 AND uPassword = #{uPassword }  
		 
	</select>
	
	<insert id="join" parameterType="User_info">
	    INSERT INTO user_info(   uCode, uEmail, uPassword, uNickname, uBirth, uGender, uNation )
	                 values (uCode_SEQ.NEXTVAL, #{uEmail}, #{uPassword}, #{uNickname}, TO_CHAR(#{uBirth},'yyyymmdd'), #{uGender}, #{uNation} )
   
    </insert>
    <select id="checkEmail" parameterType="String" resultType="int">
         SELECT count(*)
         FROM USER_INFO
         WHERE UEMAIL=#{UEMAIL}
</select>
	</mapper>